import { RoleBadge, RoleCard } from '../components/Common';

export default function AdminView({ users, boards, loggedUser, onRoleChange, onCreateBoard, onDeleteBoard }) {
  return <section className="page-view"><div className="page-heading"><div><p className="eyebrow">Control total</p><h2>Panel de administración</h2></div><RoleBadge role="admin" /></div><div className="panel-card"><p className="mini-label">Gestionar roles</p><div className="admin-list">{users.map((user) => <div className="admin-user-row" key={user.id}><RoleCard user={user} /><select value={user.role} onChange={(event) => onRoleChange(user.id, event.target.value)} disabled={user.id === loggedUser.id}><option value="member">Miembro</option><option value="moderator">Moderador</option><option value="support">Soporte</option><option value="admin">Administrador</option><option value="creator">Creador</option></select></div>)}</div></div><form className="panel-card board-form" onSubmit={onCreateBoard}><p className="mini-label">Crear una tabla</p><input name="name" placeholder="Nombre de la tabla" /><textarea name="description" placeholder="Descripción" rows="3" /><button className="primary-btn">Guardar tabla</button></form><div className="panel-card"><p className="mini-label">Gestionar tablas</p><div className="admin-list">{boards.map((board) => <div className="admin-user-row" key={board.id}><div><strong>{board.name}</strong><small>{board.description}</small></div><button className="mini-action danger-text" onClick={() => onDeleteBoard(board)}>Eliminar tabla</button></div>)}</div></div></section>;
}
